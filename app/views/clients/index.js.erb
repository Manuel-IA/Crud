$('#clients').html("<%= j render @clients %>");
$('.pagination').replaceWith('<%= j paginate @clients, window: 1, remote: true %>')
if (!$('.pagination').length){
   $('#pages').html('<%= j paginate @clients, window: 1, remote: true %>')
}


